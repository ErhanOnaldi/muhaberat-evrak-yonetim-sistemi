@{
    ViewData["Title"] = "Erişim Engellendi";
}

<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-lg-6 col-md-8">
            <div class="card shadow border-left-danger">
                <div class="card-body text-center py-5">
                    <div class="error-icon mb-4">
                        <i class="fas fa-exclamation-triangle text-danger" style="font-size: 4rem;"></i>
                    </div>
                    
                    <h1 class="h2 text-gray-900 mb-3">Erişim Engellendi</h1>
                    
                    <p class="text-gray-600 mb-4">
                        Bu sayfaya erişim yetkiniz bulunmamaktadır. Bu işlemi gerçekleştirmek için gerekli izinlere sahip değilsiniz.
                    </p>
                    
                    <div class="alert alert-warning mb-4">
                        <h6 class="alert-heading">
                            <i class="fas fa-info-circle mr-2"></i>Ne yapabilirsiniz?
                        </h6>
                        <ul class="mb-0 text-left">
                            <li>Ana sayfaya geri dönebilirsiniz</li>
                            <li>Sistem yöneticinize başvurabilirsiniz</li>
                            <li>Gerekli yetkilerin verilmesini talep edebilirsiniz</li>
                        </ul>
                    </div>
                    
                    <div class="mb-4">
                        <a asp-controller="Home" asp-action="Index" class="btn btn-primary mr-3">
                            <i class="fas fa-home mr-2"></i>Ana Sayfaya Dön
                        </a>
                        <a href="javascript:history.back()" class="btn btn-secondary">
                            <i class="fas fa-arrow-left mr-2"></i>Geri Git
                        </a>
                    </div>
                    
                    <div class="contact-info">
                        <small class="text-muted">
                            <i class="fas fa-phone mr-1"></i>Yardım için sistem yöneticinize başvurun
                        </small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .error-icon {
        animation: pulse 2s infinite;
    }
    
    @@keyframes pulse {
        0% {
            transform: scale(1);
        }
        50% {
            transform: scale(1.05);
        }
        100% {
            transform: scale(1);
        }
    }
    
    .border-left-danger {
        border-left: 0.25rem solid #e74a3b !important;
    }
</style>